# Travel Skill Reference

## 1. 项目定位

`travel-skill` 是一个面向城市旅游场景的 Agent Skill，目标不是机械罗列景点，而是根据用户的时间、预算、人流偏好和已知线索，输出更适合执行的旅游建议。

第一阶段采用 **轻量规则版 MVP + 工具增强** 方案：

- 常规推荐：走规则筛选与场景化输出
- 天气查询：走天气工具
- 路线规划：走高德工具
- 最新信息：走联网搜索工具
- 开发维护：走版本快照与回滚工具

---

## 2. 当前能力边界

### 已支持

- 城市维度的景点推荐
- 城市维度的美食推荐
- 玩 + 吃组合推荐
- 基于人流偏好的差异化输出
- 基于时长、预算、出行类型的轻量适配
- 利用用户上传图片或文字中的显式信息进行修正
- 天气查询与出游建议摘要
- 路线规划（步行 / 驾车 / 公交）
- 联网检索最新开放时间、门票、预约政策等信息
- Skill 文件快照、版本记录与回滚

### 暂不重点支持

- 实时精确人流计算
- 实时排队分钟数
- 动态票价监控
- 多城市联游自动拆解
- 酒店与交通一体化规划
- 复杂地图可视化导航

---

## 3. 请求路由机制

当前 Skill 在进入推荐逻辑前，会先判断用户请求属于哪一类任务。

### 3.1 支持的请求类型

| 请求类型 | 说明 | 典型问题 |
|----------|------|----------|
| `recommendation` | 常规旅游推荐 | 杭州半天怎么玩？ |
| `weather` | 天气与出游建议 | 上海这两天天气怎么样？ |
| `route` | 起终点路线规划 | 从橘子洲到岳麓山怎么走？ |
| `web_search` | 最新信息联网检索 | 上海博物馆最新开放时间是什么？ |

### 3.2 路由原则

- 问天气、气温、是否适合出游 → `weather`
- 问从哪到哪怎么走、步行/公交/驾车路线 → `route`
- 问最新、官网、营业时间、票价、政策 → `web_search`
- 其余旅游规划问题 → `recommendation`

### 3.3 路由输出原则

- 工具型请求返回结构化结果
- 推荐型请求返回标准 profile + 输出骨架
- 若参数不足，优先返回清晰错误信息，而不是编造结果

---

## 4. 输入解析规范

### 4.1 标准输入结构

建议将用户输入整理为：

```json
{
  "city": "上海",
  "intent": "都要",
  "crowd_preference": "人少",
  "duration": "一天",
  "travel_type": "情侣",
  "budget": "300-500",
  "first_time": true,
  "known_info": ["想拍夜景", "不想排太久队"],
  "request_type": "recommendation"
}
```

### 4.2 缺省值规则

| 字段 | 默认值 |
|------|--------|
| `intent` | 都要 |
| `crowd_preference` | 适中 |
| `duration` | 一天 |
| `travel_type` | 朋友 |
| `budget` | 未设置 |
| `first_time` | 未知 |
| `request_type` | recommendation |

### 4.3 图片 / 文字补充信息的使用优先级

按以下顺序利用用户信息：

1. 用户明确说出的需求
2. 图片 / 截图中可直接确认的线索
3. 文字补充中的隐含偏好
4. 默认规则

如果图片信息不明确，不要把模糊猜测当成事实。

---

## 5. 人流等级定义

统一使用以下等级：

| 等级 | 含义 | 用户理解 |
|------|------|----------|
| `S` | 超级热门 | 大概率人多，需要排队或错峰 |
| `A` | 热门 | 常规热门点，周末和节假日较拥挤 |
| `B` | 中等 | 人气不错，但多数时间还可接受 |
| `C` | 小众 | 相对安静，适合避开主流人流 |
| `D` | 冷门 | 非常安静，适合明确追求人少的用户 |

### 偏好映射规则

| 用户偏好 | 优先等级 | 可接受等级 | 尽量回避 |
|----------|----------|------------|----------|
| 热闹 | S、A | B | D |
| 适中 | A、B | C | S、D |
| 人少 | C、D | B | S |

说明：

- 当城市本身热门点较多时，可以保留少量不完全匹配的点，但必须提醒用户人流风险
- 第一次来城市的用户，不建议全部给 D 级点，避免“过于冷门导致体验不稳”

---

## 6. 数据模型

### 6.1 景点字段

```json
{
  "name": "景点名称",
  "city": "城市",
  "district": "所在区域",
  "category": "景点类型",
  "crowd_level": "S/A/B/C/D",
  "ticket_price": 0,
  "duration": "1-2h",
  "best_time": ["上午", "下午", "晚上"],
  "suitable_for": ["情侣", "朋友", "家庭", "独行"],
  "tags": ["夜景", "拍照", "首次到访"],
  "nearby_food_areas": ["商圈A", "街区B"],
  "tips": ["建议错峰前往", "节假日排队明显"]
}
```

### 6.2 餐厅字段

```json
{
  "name": "店名",
  "city": "城市",
  "district": "所在区域",
  "food_category": "美食类型",
  "avg_price": 80,
  "crowd_level": "A",
  "queue_level": "高/中/低",
  "taste_tags": ["本地菜", "适合聚餐"],
  "suitable_time": ["午餐", "晚餐", "夜宵"],
  "nearby_attractions": ["景点A", "景点B"],
  "recommended_dishes": ["招牌菜1", "招牌菜2"],
  "tips": ["晚高峰需要排队", "招牌菜售完较快"]
}
```

### 6.3 路线字段

```json
{
  "route_name": "路线名称",
  "city": "城市",
  "style": "半日/一日/晚上逛吃",
  "crowd_preference": "热闹/适中/人少",
  "items": [
    {
      "type": "attraction/food",
      "name": "地点名",
      "reason": "为什么放进这条路线"
    }
  ],
  "duration": "半天",
  "estimated_cost": 260
}
```

### 6.4 工具输出字段

#### 天气结果

```json
{
  "city": "杭州",
  "current": {
    "weather": "多云",
    "temperature": "26",
    "humidity": "74",
    "wind_direction": "东南",
    "wind_power": "3级",
    "report_time": "2026-04-16T10:00:00+08:00"
  },
  "forecast": [],
  "travel_advice": ["当前天气整体适合常规出游。"]
}
```

#### 路线结果

```json
{
  "mode": "walking",
  "origin": "橘子洲",
  "destination": "岳麓山",
  "distance_m": 1800,
  "duration_s": 1500,
  "summary": "walking 约 25 分钟，1.8 公里"
}
```

#### 联网搜索结果

```json
{
  "query": "上海博物馆 最新开放时间 预约政策",
  "items": [
    {
      "title": "上海博物馆",
      "snippet": "...",
      "url": "https://..."
    }
  ],
  "summary": "检索结果中的首条摘要"
}
```

---

## 7. 推荐逻辑

推荐逻辑采用“筛选 + 轻量打分”方式。

### 7.1 景点评分参考

总原则：优先选“适合当前任务且容易执行”的点。

| 维度 | 说明 |
|------|------|
| 城市匹配 | 非当前城市直接排除 |
| 人流匹配 | 与用户偏好一致优先 |
| 时间匹配 | 半天优先短平快点位，一天可组合多点 |
| 人群匹配 | 情侣、家庭、独行等适配度 |
| 预算匹配 | 高门票项目要考虑预算 |
| 首次到访适配 | 第一次来优先保留代表性景点 |

### 7.2 餐厅评分参考

| 维度 | 说明 |
|------|------|
| 城市匹配 | 非当前城市排除 |
| 与路线距离匹配 | 靠近已推荐景点优先 |
| 人流/排队匹配 | 不符合偏好时降低优先级 |
| 价格匹配 | 与预算大致匹配 |
| 本地特色 | 有城市代表性时加分 |
| 时间段匹配 | 午餐/晚餐/夜宵场景是否合适 |

---

## 8. 工具层设计

当前 `tools/` 目录采用分层标准版：

```text
tools/
├── config.py
├── tool_types.py
├── weather_client.py
├── amap_client.py
├── web_search.py
├── snapshot_store.py
└── version_manager.py
```

### 8.1 `config.py`

统一管理：

- API Key
- 请求超时
- 版本快照目录
- 默认搜索参数
- Skill 根目录配置

### 8.2 `weather_client.py`

负责：

- 城市天气查询
- 未来 3 天天气预报
- 旅游建议摘要

### 8.3 `amap_client.py`

负责：

- 地理编码
- POI 搜索
- 附近搜索
- 步行 / 驾车 / 公交路线规划

### 8.4 `web_search.py`

负责：

- 检索开放时间、门票、预约政策等易变信息
- 给推荐逻辑补充实时外部信息
- 作为静态知识库的兜底补充器

### 8.5 `snapshot_store.py` + `version_manager.py`

负责：

- 保存 Skill 快照
- 管理版本元数据
- 列出历史版本
- 回滚到指定版本

---

## 9. 路线组合规则

### 9.1 半天路线

适用于：

- 只有半天时间
- 到达日或返程日
- 用户只想轻松逛一逛

建议结构：

1. 1~2 个核心景点
2. 1 家顺路餐厅
3. 总时长控制在 4~6 小时

### 9.2 一日路线

建议结构：

1. 上午 1 个点
2. 中午 1 家餐厅
3. 下午 1~2 个点
4. 晚上可追加 1 个夜游 / 商圈 / 宵夜选项

### 9.3 晚上逛 + 晚饭

建议结构：

1. 夜景或夜游街区
2. 附近晚餐或夜宵
3. 强调灯光、氛围、排队情况和结束时间

---

## 10. 输出模板建议

### 10.1 玩推荐模板

- 总结一句：这条推荐更适合谁
- 景点 3~5 个
- 每个点包含：区域 / 类型 / 门票 / 时长 / 推荐理由 / 人流提醒
- 最后补充一句怎么安排更顺

### 10.2 吃推荐模板

- 总结一句：这组餐厅更适合什么需求
- 餐厅 3~5 家
- 每家包含：区域 / 人均 / 推荐菜 / 排队情况 / 推荐理由 / 踩坑提醒

### 10.3 玩 + 吃组合模板

建议输出四段：

1. 适合你当前需求的总判断
2. 一条主路线
3. 备选方案
4. 避坑提醒

### 10.4 工具型模板

#### 天气

- 天气概览
- 未来预报
- 出行建议

#### 路线

- 路线概览
- 起终点
- 时长和距离
- 补充建议

#### 联网搜索

- 搜索结论
- 结果列表
- 使用建议

---

## 11. MVP 数据准备建议

第一阶段建议只支持 1~3 个城市。

### 推荐数据规模

- 每个城市 15~20 个景点
- 每个城市 20~30 家餐厅
- 每个城市至少准备：
  - 热闹路线样本
  - 适中路线样本
  - 人少路线样本

### 推荐字段优先级

#### 景点最少要有

- 名称
- 区域
- 类型
- 人流等级
- 门票价格
- 建议时长
- 适合时段
- 推荐理由

#### 餐厅最少要有

- 店名
- 区域
- 美食类型
- 人均价格
- 推荐菜
- 排队情况
- 人流等级
- 推荐理由

---

## 12. 实施建议

如果需要后续扩展到更强版本，优先迭代顺序建议为：

1. 先补城市数据
2. 再增强图片 / 文字输入提取
3. 再增强推荐结果生成能力
4. 再引入预算模式和天气联动
5. 最后再考虑动态热度和地图能力
